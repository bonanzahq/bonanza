# Journal: 2026-02-08

## Session Summary

Plan review and cleanup session. All 14 migration plans from October 2025 were audited against a review document, then updated to fix staleness, contradictions, and bloat. Also set up project AGENTS.md and switched issue tracking from beads to git-bug.

## Activities

### 1. Plan Review Audit

Read `docs/plans/plan-review-2026-02-08.md` which identified issues across all plan files. Plans were 4 months stale (written October 2025).

### 2. Clarified Decisions with Fabian

- **a3 (Testing)**: Use transactional fixtures only, drop database_cleaner
- **c2 (Email)**: Use `.deliver_later` everywhere
- **c3 (Conduct)**: Keep 2 warnings / 30 days auto-ban rule
- **Execution order**: Move testing (a3) before dependency upgrades (a2)

### 3. Plan Fixes (10 files, -477 net lines)

| File | Change |
|------|--------|
| b4 | Replaced `philippgeuder` with `bonanzahq` (8 occurrences) |
| a1 | Removed dead CLAUDE.md/AGENTS.md refs, noted yarn.lock already deleted |
| c1 | Updated solid_queue `~> 0.3` to `~> 1.0`, noted Rails 8 integration |
| c2 | Changed all `.deliver_now` to `.deliver_later`, rewrote contradictory section |
| a3 | Removed database_cleaner, switched to transactional fixtures |
| b2 | Made `rescue_from StandardError` production-only |
| b1 | Updated base image from Ruby 3.1.2 to 3.4, removed CLAUDE.md ref |
| a2 | Trimmed ~460 lines (comms templates, budget, alt paths, future scope), updated versions, fixed yarn->pnpm in code examples |
| d1 | Added schema dependency note (must verify after a2 completes) |
| EXECUTION-ORDER | Reordered: testing now precedes dependency upgrades |

### 4. Version Research

- Ruby 3.5 released Dec 2025, Ruby 3.1 EOL Jan 2026, Ruby 3.2 EOL Mar 2026
- Rails 8.0.4 released Oct 2025, Rails 8.1.x also available
- Solid Queue at 1.3.x (stable)
- Updated all plans to target Ruby 3.4.x (or 3.5.x) and Rails 8.0.4+ or 8.1.x

### 5. Created AGENTS.md + CLAUDE.md

Created project-level `AGENTS.md` covering tech stack, core models, common commands, authorization model, and migration plan references. Symlinked `CLAUDE.md -> AGENTS.md`.

### 6. Switched Issue Tracking to git-bug

Replaced beads references in `~/.claude/CLAUDE.md` with git-bug commands. Created Claude identity in git-bug. Filed 5 open issues for outstanding work.

## Technical Decisions

1. **Transactional fixtures over database_cleaner** -- simpler, Rails built-in, sufficient for Minitest
2. **deliver_later everywhere** -- consistent with Solid Queue background jobs plan
3. **Testing before upgrades** -- tests must exist to verify the upgrade doesn't break things
4. **git-bug over beads** -- distributed bug tracking embedded in git, fewer rough edges

## Open Issues (git-bug)

- `cdcb38b` Write docs/SPEC.md
- `ad8e3b5` Clean up docs/bonanza-dev-notes/ (Fabian's decision)
- `affa0bf` Execute a1: Yarn to pnpm migration
- `c63b807` Execute a3: Testing infrastructure
- `8d0fc12` Execute a2: Dependency updates (blocked by a3)

## Files Created

- `docs/plans/plan-review-2026-02-08.md` -- plan review notes
- `AGENTS.md` -- project instructions for AI assistants
- `CLAUDE.md` -- symlink to AGENTS.md
- `docs/journals/2026-02-08.md` -- this journal

## Files Modified

- 10 plan files (see table above)
- `~/.claude/CLAUDE.md` -- beads -> git-bug

## Context for Next Session

Plans are now clean and ready for execution. Next implementation work per EXECUTION-ORDER.md:

1. **a1 (pnpm)** -- half day, partially done. Procfile.dev still says yarn.
2. **a3 (testing)** -- 1 week. Framework setup, factories, core model tests.
3. **a2 (deps)** -- 2-3 weeks, blocked by a3. Ruby 3.4+, Rails 8.x.

Also outstanding: `docs/SPEC.md` needs writing, `docs/bonanza-dev-notes/` needs Fabian's cleanup decision.
