# Journal: 2026-02-09

## Session Summary

Issue tracking overhaul, system specification, and infrastructure fixes. Set up git-bug labels and GitHub bridge sync, wrote docs/SPEC.md, fixed GPG signing for the VM, and created PR #19 to merge wip/repo-setup into main.

## Activities

### 1. git-bug Label Audit and Restructuring

Reviewed all 18 open issues. Found no work state labels and only basic category labels. Restructured:

- **Removed**: `housekeeping` (replaced by proper type labels)
- **Added type labels**: `bug`, `feature`, `enhancement`, `chore`, `documentation` (alongside existing `epic`)
- **Added work state labels**: `ready` (4 issues), `blocked` (10 issues), epics left without work state
- **Added phase labels**: already existed (`phase-a` through `phase-d`)

### 2. GitHub Bridge Setup

- Configured git-bug GitHub bridge (`git bug bridge new`) with fmzbot identity
- Pushed all 18 issues to GitHub Issues on bonanzahq/bonanza
- Labels sync bidirectionally

### 3. Updated AGENTS.md

Added Issue Tracking section covering: git-bug usage, skill reference, label taxonomy (type/phase/work state), and session workflow including GitHub bridge sync.

### 4. GPG Signing Fix

`SSH_AUTH_SOCK` was not set in the VM -- SSH agent was running but not exported to our shell. Fixed by locating the socket at `/tmp/ssh-WMvnGWHbpxNJ/agent.1542`.

GPG signing was failing because `pinentry-mac` requires a GUI. Replaced the passphrase-protected key with a new passphrase-less ed25519 key (`847A44486980BA6F`). Uploaded to GitHub. Old key (`0AA64A9B5D842516`) still on GitHub -- should be removed.

Also set `git config --global user.name/email` to fmzbot/o_o@fmz.one (was unset).

### 5. Wrote docs/SPEC.md

Explored the full codebase (models, controllers, routes, schema, config, locales) with three parallel agents. Wrote high-level specification covering:

- Overview, terminology (German UI terms)
- Technical overview (stack, deployment architecture)
- User roles and authorization
- Core workflows (borrower registration, lending checkout, returns, conduct)
- Domain model (10 entities)
- Business rules, search, legal content

### 6. Created PR #19

PR: `wip/repo-setup` -> `main` covering all work since initial commit (repo setup, AGENTS.md, plans, SPEC.md, issue tracking).

### 7. Filed New Issue

- `8edcef6` fix: Replace outdated FHP logo (`bug`, `ready`)

## Technical Decisions

1. **Passphrase-less GPG key** -- acceptable for a bot account on a VM, avoids pinentry issues
2. **SPEC.md stays domain-level with light tech overview** -- implementation details belong in docs/structure/
3. **git-bug as source of truth, GitHub Issues as mirror** -- bridge push syncs issues to GitHub for visibility

## Infrastructure Notes

- SSH agent socket: `/tmp/ssh-WMvnGWHbpxNJ/agent.1542` (needed for git push/pull and git-bug sync)
- Old GPG key `0AA64A9B5D842516` should be removed from GitHub (private key deleted locally)

## Open Issues (git-bug)

Closed this session:
- `cdcb38b` Write docs/SPEC.md -- CLOSED

Still open and ready:
- `ad8e3b5` Clean up docs/bonanza-dev-notes/
- `affa0bf` Execute a1: Yarn to pnpm migration
- `c63b807` Execute a3: Testing infrastructure
- `8edcef6` fix: Replace outdated FHP logo

## Context for Next Session

PR #19 pending merge to main. After merge:
1. Start feature branch for a1 (pnpm migration) -- half day effort
2. a3 (testing infrastructure) follows -- ~1 week
3. a2 (dependency upgrades) blocked by a3
